# Configuration file for fw_printenv/fw_setenv
# Syntax: device_name offset env_size flash_sector_size
#
# BeagleBone Black - U-Boot environment on FAT partition (uboot.env file)
# Device: /dev/mmcblk0p1 (boot partition)
# U-Boot saves environment to file "uboot.env" on FAT partition

# For U-Boot 2022.01 on BeagleBone Black with FAT boot partition
# The environment is stored in a file on the FAT filesystem
/boot/uboot.env 0x0000 0x20000
